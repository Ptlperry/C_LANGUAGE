#include <stdio.h>
#include <conio.h>

main() {
    int a = 12, b = 6, add;
    int c = 12, d = 6, sub;
    int p = 12, q = 6, mul;
    int x = 12, y = 6, div;
    int A = 12, B = 6, per;

    


    add = a + b;
    printf("Addition of 12 and 6 is %d",add);

    sub = c - d;
    printf("\nSubtraction of 12 and 6 is %d",sub);

    mul = p * q;
    printf("\nMultiplication of 12 and 6 is %d",mul);

    div = x / y;
    printf("\nDivision of 12 and 6 is %d",div);

    per = A % B;
    printf("\nModulo of 12 and 6 is %d",per);

    add = a + b;
    printf("\n12 + 6 = %d",add);
 
    sub = c - d;
    printf("\n12 - 6 = %d",sub);

    mul = p * q;
    printf("\n12 * 6 = %d",mul);

    div = x / y;
    printf("\n12 / 6 = %d",div);

    per = A % B;
    printf("\n12  6 = %d",per);

}

